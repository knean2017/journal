import { NotFoundBody } from '@/components/ui/NotFoundBody'

/** `notFound()` from an article or contributor page. The chrome is the layout's. */
export default function SiteNotFound() {
  return <NotFoundBody />
}
